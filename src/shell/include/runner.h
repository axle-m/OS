#include "parser.h"

void cmd_echo(parsed_command_t *cmd);
void cmd_help_print(int command_id);
void cmd_help(parsed_command_t *cmd);
void cmd_clear(parsed_command_t *cmd);
void cmd_shutdown(parsed_command_t *cmd);
